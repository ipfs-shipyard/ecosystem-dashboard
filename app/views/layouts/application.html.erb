<!DOCTYPE html>
<html class='h-100'>
  <head>
    <title>IPFS Ecosystem Research</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class='d-flex flex-column h-100'>
    <header>
      <nav class="navbar navbar-dark bg-dark navbar-fixed-top navbar-expand-md">
        <a class="navbar-brand" href="/">IPFS Ecosystem Research</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item">
              <%= link_to 'Collabs', collabs_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'This Week', weekly_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'All Issues+PRs', all_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Protocol Orgs', orgs_path, class: 'nav-link' %>
            </li>
          </ul>
        </div>
      </nav>
    </header>

    <main role="main" class="flex-shrink-0">
      <div class="container">
        <% flash.each do |key, value| %>
          <div class="<%= flash_class(key) %>">
            <%= value %>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </main>
  </body>
</html>
