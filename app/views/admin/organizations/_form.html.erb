<%= simple_form_for [:admin,@organization] do |f| %>
  <%= f.input :name, label: 'Github Username' %>
  <%= f.input :internal %>
  <%= f.input :collaborator %>
  <%= f.input :docker_hub_org, label: 'Docker Org Username' %>
  <%= f.button :submit %>
<% end %>
