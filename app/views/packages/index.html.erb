<nav class="mb-2 mt-2 navbar navbar-expand-lg navbar-light">
  <span class="navbar-brand" >
    <strong>Internal Packages</strong>
  </span>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%= render partial: 'issues/excludable_filter', locals: {filter_name: 'org', filter_label: 'Organisation', filter_collection: @orgs} if Organization.internal.count > 1 %>
      <%= render partial: 'issues/excludable_filter', locals: {filter_name: 'platform', filter_label: 'Platform', filter_collection: @platforms} %>
    </ul>
  </div>
</nav>

<%= render @packages %>
<%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>
